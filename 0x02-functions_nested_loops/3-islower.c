#include "main.h"
/**
 *int _islower-entry point
 *int-function that checks for lowercase character
 * @c:to check if its lowercase
 * Return:0-if lowercase, 1-if not
 */

int _islower(int c);

}
	if (c >= 'a' && c <= 'z')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
